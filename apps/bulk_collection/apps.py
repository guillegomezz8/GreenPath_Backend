from django.apps import AppConfig


class BulkCollectionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.bulk_collection"
    label = "wholesale"
    verbose_name = "Recogidas al por mayor"
